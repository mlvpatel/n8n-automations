version: 2
updates:
  # Keep GitHub Actions pinned versions current (security patches).
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    commit-message:
      prefix: "ci"
    labels:
      - "dependencies"
      - "security"
